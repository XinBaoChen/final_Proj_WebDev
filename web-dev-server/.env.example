# Example local env for web-dev-server
# Docker/Postgres helper files were removed. The app now uses a local
# SQLite file by default when `DATABASE_URL` is not set.

# To explicitly use a hosted Postgres, set DATABASE_URL, for example:
# DATABASE_URL=postgres://user:pass@host:5432/dbname

# To control the local sqlite file path (optional):
# SQLITE_STORAGE=./dev.sqlite

# Useful dev flags
# FORCE_SYNC=true
# SEED_DB=true
# SKIP_CREATE_DB=true
